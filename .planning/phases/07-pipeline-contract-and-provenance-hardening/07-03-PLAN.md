---
phase: "07-pipeline-contract-and-provenance-hardening"
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/pipelines/run_common_data_pipeline.m
  - src/contracts/build_pipeline_provenance.m
  - src/output/save_run_manifest.m
  - docs/data_format.md
  - docs/pipeline_notes.md
  - docs/architecture_overview.md
  - tests/test_real_pipeline.m
  - tests/test_sim_pipeline.m
autonomous: true
requirements:
  - ARCH-03
  - RUN-01
  - RUN-03
  - DOC-01
  - TEST-01
user_setup: []
---

<objective>
Add dual-track stage and provenance outputs while preserving all legacy result fields.

Purpose: give future code a cleaner result contract without breaking existing scripts.

Output: `result.stages`, `result.provenance`, manifest integration, docs, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-CONTEXT.md
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-RESEARCH.md
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-02-SUMMARY.md
@src/pipelines/run_common_data_pipeline.m
@src/contracts/build_pipeline_provenance.m
@src/output/save_run_manifest.m
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate result.stages without deleting legacy fields</name>
  <files>src/pipelines/run_common_data_pipeline.m, docs/pipeline_notes.md</files>
  <read_first>src/pipelines/run_common_data_pipeline.m</read_first>
  <action>
  Add `result.stages` as a compact stage summary while keeping existing fields unchanged.
  Suggested fields:
  `result.stages.source`, `result.stages.degradation`, `result.stages.recovery`, `result.stages.imaging`, `result.stages.analysis`, `result.stages.recoveryEvaluation`, and `result.stages.output`.
  Each stage should include status, key counts, elapsed time if available, method/mode if applicable, and output files if applicable.
  Do not duplicate large arrays inside `result.stages`.
  </action>
  <verify>Get-Content src\pipelines\run_common_data_pipeline.m | Select-String -Pattern "result.stages","degradation","recoveryEvaluation","output"</verify>
  <acceptance_criteria>
    - Legacy fields still exist.
    - `result.stages` exists for real and sim.
    - `result.stages` is compact and readable.
  </acceptance_criteria>
  <done>Pipeline results expose a stable stage-oriented view.</done>
</task>

<task type="auto">
  <name>Task 2: Populate result.provenance and manifest from the same data</name>
  <files>src/pipelines/run_common_data_pipeline.m, src/contracts/build_pipeline_provenance.m, src/output/save_run_manifest.m</files>
  <read_first>src/contracts/build_pipeline_provenance.m, src/output/save_run_manifest.m</read_first>
  <action>
  Call `build_pipeline_provenance` near the end of common pipeline and assign `result.provenance`.
  Feed the same provenance into `save_run_manifest` so manifest content and in-memory result stay aligned.
  Include data identity, pipeline mode, config mode, cache key/folder, degradation mode/missing counts, recovery status/method, imaging grid, analysis status, recovery evaluation status, backend, and run directory.
  </action>
  <verify>Get-Content src\pipelines\run_common_data_pipeline.m, src\contracts\build_pipeline_provenance.m, src\output\save_run_manifest.m | Select-String -Pattern "result.provenance","save_run_manifest","cache","backend"</verify>
  <acceptance_criteria>
    - `result.provenance` exists for real and sim.
    - Manifest and `result.provenance` are generated from consistent values.
    - Provenance excludes large matrices.
  </acceptance_criteria>
  <done>Run trace is available both in memory and on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Document the transition contract and test both tracks</name>
  <files>docs/data_format.md, docs/pipeline_notes.md, docs/architecture_overview.md, tests/test_real_pipeline.m, tests/test_sim_pipeline.m</files>
  <read_first>docs/pipeline_notes.md, tests/test_real_pipeline.m, tests/test_sim_pipeline.m</read_first>
  <action>
  Update docs to explain:
  legacy fields remain supported in Phase 7;
  new code should prefer `result.stages` and `result.provenance`;
  old fields may be deprecated in a later phase only after explicit migration.
  Add tests that compare key legacy fields with the new stage/provenance summaries.
  </action>
  <verify>matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests({'tests/test_real_pipeline.m','tests/test_sim_pipeline.m'}); assert(all([results.Passed]));"</verify>
  <acceptance_criteria>
    - Docs clearly describe dual-track compatibility.
    - Tests verify legacy and new fields agree on key statuses/counts.
    - No old fields are removed.
  </acceptance_criteria>
  <done>The project has a safe migration path toward structured pipeline results.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --check`
- [ ] `matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests('tests'); assert(all([results.Passed]), 'Some tests failed.');"`
- [ ] `result.degradation`, `result.recovery`, `result.image`, `result.analysis` still exist.
- [ ] `result.stages` and `result.provenance` exist for real and sim pipeline outputs.
- [ ] Run manifest is written only when output saving is enabled.
</verification>

<success_criteria>
- Dual-track result structure is implemented.
- Manifest/provenance/stages are aligned.
- Legacy fields remain supported.
- Full tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pipeline-contract-and-provenance-hardening/07-03-SUMMARY.md`
</output>

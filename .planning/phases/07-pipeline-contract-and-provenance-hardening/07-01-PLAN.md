---
phase: "07-pipeline-contract-and-provenance-hardening"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/validate_source_data.m
  - src/contracts/build_pipeline_provenance.m
  - src/output/save_run_manifest.m
  - docs/data_format.md
  - docs/pipeline_notes.md
  - tests/test_contract_normalization.m
  - tests/test_real_pipeline.m
  - tests/test_sim_pipeline.m
autonomous: true
requirements:
  - ARCH-03
  - DOC-01
  - RUN-01
  - TEST-01
user_setup: []
---

<objective>
Strengthen the sourceData contract and add run provenance/manifest foundations before changing pipeline control flow.

Purpose: make the internal data language and run trace explicit with minimal risk.

Output: stronger `validate_source_data`, new provenance builder, new run manifest saver, updated docs, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-CONTEXT.md
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-RESEARCH.md
@src/contracts/normalize_source_data.m
@src/contracts/validate_source_data.m
@src/output/prepare_run_dir.m
@src/output/save_summary.m
@docs/data_format.md
@docs/pipeline_notes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen sourceData validation</name>
  <files>src/contracts/validate_source_data.m, docs/data_format.md, tests/test_contract_normalization.m</files>
  <read_first>src/contracts/normalize_source_data.m, src/data/real/load_gotcha_data.m, src/data/sim/generate_point_target_data.m</read_first>
  <action>
  Extend `validate_source_data` around fields the project already produces:
  required top-level fields `track`, `echo`, `radar`, `mask`, `meta`;
  required track fields `x/y/z`;
  required radar fields used by BP/recovery such as `c`, `centerOmega`, `numRangeSamples`, `numRangeSamplesUp`, and `rangeStep`;
  numeric finite checks for track/radar scalar fields;
  logical/vector mask normalization and shape validation;
  echo dimension checks.
  Keep error messages specific to the failing field path.
  Update `docs/data_format.md` with units, dimensions, required fields, and optional metadata.
  </action>
  <verify>matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests('tests/test_contract_normalization.m'); assert(all([results.Passed]));"</verify>
  <acceptance_criteria>
    - Invalid sourceData fails before BP/recovery runs.
    - Existing real/sim generated sourceData still passes.
    - Data format doc describes units and dimensions clearly.
  </acceptance_criteria>
  <done>The sourceData contract is stricter without breaking current normalized data.</done>
</task>

<task type="auto">
  <name>Task 2: Add compact pipeline provenance builder</name>
  <files>src/contracts/build_pipeline_provenance.m, docs/pipeline_notes.md, tests/test_real_pipeline.m, tests/test_sim_pipeline.m</files>
  <read_first>src/pipelines/run_real_data_pipeline.m, src/pipelines/run_sim_data_pipeline.m</read_first>
  <action>
  Add `build_pipeline_provenance(context)` to create a compact struct for stage traceability.
  It should record mode, source kind/provider, data root or generator name, echo size, azimuth count, mask count, degradation status, recovery status/method, imaging grid, reference cache info, recovery evaluation status, runtime backend, and run directory.
  Do not store full echo matrices or image matrices in provenance.
  Keep the function tolerant of missing optional fields so it can be used incrementally.
  </action>
  <verify>Get-Content src\contracts\build_pipeline_provenance.m | Select-String -Pattern "source","degradation","recovery","imaging","cache"</verify>
  <acceptance_criteria>
    - Provenance is compact and serializable.
    - Provenance does not duplicate large arrays.
    - Function can be called from both real and sim pipelines.
  </acceptance_criteria>
  <done>A shared provenance builder exists for later pipeline integration.</done>
</task>

<task type="auto">
  <name>Task 3: Add run manifest output</name>
  <files>src/output/save_run_manifest.m, docs/pipeline_notes.md, tests/test_real_pipeline.m, tests/test_sim_pipeline.m</files>
  <read_first>src/output/prepare_run_dir.m, src/output/save_summary.m, src/utils/safe_save.m</read_first>
  <action>
  Add `save_run_manifest(result, runInfo, config)` under `src/output`.
  It should respect `runInfo.enabled` and output settings.
  Store a small human-readable text manifest in the run directory, including timestamp, project mode, data source, config/output flags, cache info, degradation, recovery, imaging grid, and recovery evaluation status.
  If a machine-readable MAT file is useful, keep it compact and optional; do not duplicate large matrices.
  </action>
  <verify>Get-Content src\output\save_run_manifest.m | Select-String -Pattern "manifest","degradation","recovery","imaging","provenance"</verify>
  <acceptance_criteria>
    - Manifest is skipped when output saving is disabled.
    - Manifest records enough information to understand how the run was produced.
    - Manifest is small and excludes full data matrices.
  </acceptance_criteria>
  <done>Run-level provenance can be saved without affecting existing summary output.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --check`
- [ ] `matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests('tests'); assert(all([results.Passed]), 'Some tests failed.');"`
- [ ] Real and sim smoke tests still pass with output disabled.
- [ ] Manifest save path is tested with output enabled in a temp run directory.
</verification>

<success_criteria>
- `sourceData` contract is stricter and documented.
- Provenance builder exists and avoids large payload duplication.
- Run manifest output exists and is optional.
- Existing public pipeline entry points are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pipeline-contract-and-provenance-hardening/07-01-SUMMARY.md`
</output>

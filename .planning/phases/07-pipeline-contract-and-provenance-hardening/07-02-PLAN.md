---
phase: "07-pipeline-contract-and-provenance-hardening"
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/pipelines/run_common_data_pipeline.m
  - src/pipelines/run_real_data_pipeline.m
  - src/pipelines/run_sim_data_pipeline.m
  - src/contracts/build_pipeline_provenance.m
  - docs/pipeline_notes.md
  - docs/architecture_overview.md
  - tests/test_real_pipeline.m
  - tests/test_sim_pipeline.m
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
  - REAL-03
  - SIM-02
  - RUN-01
  - TEST-01
user_setup: []
---

<objective>
Extract the shared post-source processing chain into `run_common_data_pipeline` while preserving real/sim public entry points.

Purpose: reduce real/sim drift without changing how users call the project.

Output: `run_common_data_pipeline(sourceData, sourceReference, config, context)` plus thin real/sim wrappers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-CONTEXT.md
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-RESEARCH.md
@.planning/phases/07-pipeline-contract-and-provenance-hardening/07-01-SUMMARY.md
@src/pipelines/run_real_data_pipeline.m
@src/pipelines/run_sim_data_pipeline.m
@src/data/degradation/apply_degradation.m
@src/recovery/run_recovery.m
@src/bp_core/bp_imaging.m
@src/analysis/point_target_analysis.m
@src/analysis/run_recovery_evaluation.m
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design common pipeline input context</name>
  <files>src/pipelines/run_common_data_pipeline.m, docs/pipeline_notes.md</files>
  <read_first>src/pipelines/run_real_data_pipeline.m, src/pipelines/run_sim_data_pipeline.m</read_first>
  <action>
  Create `run_common_data_pipeline.m` with a simple input contract:
  `result = run_common_data_pipeline(sourceData, sourceReference, config, pipelineContext)`.
  `pipelineContext` should include `mode`, `runInfo`, `log`, `backend`, `poolInfo`, optional `fullImageResult`, optional `referenceCacheInfo`, and source-specific labels.
  Keep the function local to `src/pipelines`; do not create a new directory.
  </action>
  <verify>Get-Content src\pipelines\run_common_data_pipeline.m | Select-String -Pattern "sourceData","sourceReference","pipelineContext","runInfo"</verify>
  <acceptance_criteria>
    - Common pipeline input contract is explicit.
    - Real/sim-specific data construction remains outside common pipeline.
    - No public entry point signature changes.
  </acceptance_criteria>
  <done>The shared processing function has a stable boundary before logic migration.</done>
</task>

<task type="auto">
  <name>Task 2: Move shared post-source chain</name>
  <files>src/pipelines/run_common_data_pipeline.m, src/pipelines/run_real_data_pipeline.m, src/pipelines/run_sim_data_pipeline.m</files>
  <read_first>src/pipelines/run_real_data_pipeline.m, src/pipelines/run_sim_data_pipeline.m</read_first>
  <action>
  Move the shared sequence into `run_common_data_pipeline`:
  checkpoint after data load/generation, reference image preparation when required, `apply_degradation`, optional `run_recovery`, `bp_imaging`, `point_target_analysis`, optional `run_recovery_evaluation`, image/analysis/recovery saves, summary construction, manifest save, final checkpoint.
  Real and sim wrappers should only validate config, prepare run dir/logger/backend/pool, build sourceData/sourceReference, build pipelineContext, then call common pipeline.
  Preserve real behavior where full reference cache is prepared early.
  Preserve sim behavior where reference image is prepared for image-quality analysis when enabled.
  </action>
  <verify>Get-Content src\pipelines\run_real_data_pipeline.m, src\pipelines\run_sim_data_pipeline.m, src\pipelines\run_common_data_pipeline.m | Select-String -Pattern "run_common_data_pipeline","apply_degradation","run_recovery","bp_imaging","run_recovery_evaluation"</verify>
  <acceptance_criteria>
    - Real and sim wrappers are thin but still readable.
    - Existing result fields remain present.
    - Checkpoints and logs remain functionally equivalent.
    - Recovery evaluation remains optional.
  </acceptance_criteria>
  <done>Real/sim post-source processing now follows one shared implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests for behavior preservation</name>
  <files>tests/test_real_pipeline.m, tests/test_sim_pipeline.m, docs/architecture_overview.md, docs/pipeline_notes.md</files>
  <read_first>tests/test_real_pipeline.m, tests/test_sim_pipeline.m</read_first>
  <action>
  Update tests so they assert the same legacy fields still exist after common pipeline extraction.
  Add checks that `run_real_data_pipeline` and `run_sim_data_pipeline` still return expected image sizes, degradation status, recovery status, recovery evaluation status, and summary fields.
  Update docs to describe real/sim wrappers plus common post-source pipeline.
  </action>
  <verify>matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests({'tests/test_real_pipeline.m','tests/test_sim_pipeline.m'}); assert(all([results.Passed]));"</verify>
  <acceptance_criteria>
    - Existing real/sim smoke tests pass.
    - Tests cover disabled and enabled recovery evaluation paths.
    - Docs show the new common pipeline without implying a directory migration.
  </acceptance_criteria>
  <done>Common pipeline extraction is covered by regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --check`
- [ ] `matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests('tests'); assert(all([results.Passed]), 'Some tests failed.');"`
- [ ] `run_real_data_pipeline` and `run_sim_data_pipeline` signatures are unchanged.
- [ ] No large directory migration was introduced.
</verification>

<success_criteria>
- Shared post-source processing exists in `run_common_data_pipeline`.
- Real/sim wrappers remain stable external APIs.
- Behavior-preservation tests pass.
- Documentation explains the new pipeline split.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pipeline-contract-and-provenance-hardening/07-02-SUMMARY.md`
</output>

---
phase: "06-analysis-and-hardening"
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/analysis/run_recovery_evaluation.m
  - src/analysis/recovery_evaluation/prepare_recovery_evaluation_config.m
  - src/analysis/recovery_evaluation/build_recovery_evaluation_cases.m
  - src/analysis/recovery_evaluation/evaluate_recovery_cases.m
  - src/analysis/recovery_evaluation/summarize_recovery_evaluation.m
  - src/analysis/recovery_evaluation/metrics/compute_sim_recovery_metrics.m
  - src/analysis/recovery_evaluation/metrics/compute_real_recovery_metrics.m
  - src/analysis/recovery_evaluation/builders/build_case_result.m
  - src/analysis/recovery_evaluation/builders/build_comparison_result.m
autonomous: true
requirements:
  - ARCH-03
  - ANL-01
  - ANL-02
  - CS-01
  - CS-02
user_setup: []
---

<objective>
建立 recovery evaluation 的核心主链和统一结果骨架。  
Purpose: 让模块先在不接 outputs、不接 pipeline 的情况下，稳定产出 `cases / comparisons / summary` 三层结构。  
Output: 可独立调用的 recovery evaluation 主链，以及统一的 case/comparison/summary 结构。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-analysis-and-hardening/06-CONTEXT.md
@.planning/phases/06-analysis-and-hardening/06-RESEARCH.md
@src/analysis/point_target_analysis.m
@src/analysis/evaluate_image_quality.m
@src/runtime/get_full_image_reference.m
@src/recovery/run_recovery.m
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 recovery evaluation 顶层主链</name>
  <files>src/analysis/run_recovery_evaluation.m, src/analysis/recovery_evaluation/prepare_recovery_evaluation_config.m, src/analysis/recovery_evaluation/build_recovery_evaluation_cases.m</files>
  <read_first>.planning/phases/06-analysis-and-hardening/06-CONTEXT.md, src/analysis/point_target_analysis.m</read_first>
  <action>实现 recovery evaluation 单入口和前两段主链：`prepare -> build_cases`。其中 `prepare_*` 负责标准化 `evaluationMode`、comparison plan 和 outputs；`build_*` 只组织 `full / interrupted / recovered_cs_1d / recovered_cs_2d` 四组 case，不做结果封装。</action>
  <verify>Get-Content src\analysis\run_recovery_evaluation.m, src\analysis\recovery_evaluation\prepare_recovery_evaluation_config.m, src\analysis\recovery_evaluation\build_recovery_evaluation_cases.m | Select-String -Pattern "evaluationMode","comparisonPlan","recovered_cs_1d","recovered_cs_2d"</verify>
  <acceptance_criteria>
    - `run_recovery_evaluation` 成为唯一公开入口
    - `prepare_*` 和 `build_cases` 职责清楚分离
    - 四组固定 case 能被标准化组织
  </acceptance_criteria>
  <done>后续评估和 summary 都可以基于稳定的 case 集合工作。</done>
</task>

<task type="auto">
  <name>Task 2: 实现逐 case 评估与 comparison 主逻辑</name>
  <files>src/analysis/recovery_evaluation/evaluate_recovery_cases.m, src/analysis/recovery_evaluation/metrics/compute_sim_recovery_metrics.m, src/analysis/recovery_evaluation/metrics/compute_real_recovery_metrics.m, src/analysis/recovery_evaluation/builders/build_case_result.m, src/analysis/recovery_evaluation/builders/build_comparison_result.m</files>
  <read_first>src/analysis/evaluate_image_quality.m, src/analysis/point_target_analysis.m</read_first>
  <action>实现 `evaluate_recovery_cases.m`，对每个 case 做评估并生成 comparison。sim 只走 `compute_sim_recovery_metrics`，real 只走 `compute_real_recovery_metrics`。用 builders 统一封装 `caseResult` 和 `comparisonResult`，禁止到处手写散 struct。</action>
  <verify>Get-Content src\analysis\recovery_evaluation\evaluate_recovery_cases.m, src\analysis\recovery_evaluation\metrics\compute_sim_recovery_metrics.m, src\analysis\recovery_evaluation\metrics\compute_real_recovery_metrics.m | Select-String -Pattern "build_case_result","build_comparison_result","skipped","not_available"</verify>
  <acceptance_criteria>
    - sim / real 指标逻辑彻底分离
    - `cases` 与 `comparisons` 都采用统一结构
    - 不适用指标显式标记
  </acceptance_criteria>
  <done>恢复评估的核心数据骨架稳定可用。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 summary 结论层</name>
  <files>src/analysis/recovery_evaluation/summarize_recovery_evaluation.m</files>
  <read_first>src/analysis/recovery_evaluation/evaluate_recovery_cases.m, .planning/phases/06-analysis-and-hardening/06-CONTEXT.md</read_first>
  <action>实现 summary 层，只基于已有 `cases` 和 `comparisons` 给出结构化结论。summary 必须回答“是否优于 interrupted、是否接近 full、哪种方法更好、有哪些风险或限制”，不能自行重算底层指标。</action>
  <verify>Get-Content src\analysis\recovery_evaluation\summarize_recovery_evaluation.m | Select-String -Pattern "highLevelFindings","bestMethodIfAny","riskOrLimitations","modeSpecificNotes"</verify>
  <acceptance_criteria>
    - summary 输出为结构化结果
    - summary 不重算底层指标
    - summary 能区分 sim / real 的解释口径
  </acceptance_criteria>
  <done>模块已经能独立回答“恢复有没有变好、哪种恢复更好”。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 独立调用 `run_recovery_evaluation(...)` 能返回 `cases / comparisons / summary`
- [ ] sim / real 两种 `evaluationMode` 都能跑通主链
- [ ] `build_recovery_evaluation_cases` 和 `build_case_result` 没有职责穿透
</verification>

<success_criteria>
- recovery evaluation 核心主链落地
- 结果结构稳定统一
- outputs 和 pipeline 接入前就已具备可测的内部闭环
</success_criteria>

<output>
After completion, create `.planning/phases/06-analysis-and-hardening/06-02-SUMMARY.md`
</output>

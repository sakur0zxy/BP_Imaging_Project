---
phase: "06-analysis-and-hardening"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/default_real_config.m
  - config/default_sim_config.m
  - src/contracts/validate_real_config.m
  - src/contracts/validate_sim_config.m
  - docs/config_contract.md
  - docs/pipeline_notes.md
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - DOC-01
  - ANL-01
  - ANL-02
user_setup: []
---

<objective>
冻结 Phase 6 的 recovery evaluation 外部契约。  
Purpose: 先把 `analysis.recoveryEvaluation.*` 的配置结构、标准 mode、输出开关和 validators 定死，避免后续实现时字段反复漂移。  
Output: 稳定的 recovery evaluation 配置 schema、validators 和文档说明。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analysis-and-hardening/06-CONTEXT.md
@.planning/phases/06-analysis-and-hardening/06-RESEARCH.md
@config/default_real_config.m
@config/default_sim_config.m
@src/contracts/validate_real_config.m
@src/contracts/validate_sim_config.m
@docs/config_contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 recovery evaluation 配置骨架</name>
  <files>config/default_real_config.m, config/default_sim_config.m</files>
  <read_first>.planning/phases/06-analysis-and-hardening/06-CONTEXT.md, docs/config_contract.md</read_first>
  <action>在 real/sim 默认配置中新增 `config.analysis.recoveryEvaluation.*` 配置区，明确 `enable`、`evaluationMode`、固定四组 case、comparison 策略和 `outputs.saveMat/saveSummary/savePanel/showPanel` 开关。注释保持简洁但要写清楚 sim/real 口径和模块可插拔边界。</action>
  <verify>Get-Content config\default_real_config.m, config\default_sim_config.m | Select-String -Pattern "recoveryEvaluation","saveMat","saveSummary","savePanel","showPanel","evaluationMode"</verify>
  <acceptance_criteria>
    - real/sim 默认配置中都存在 recoveryEvaluation 配置区
    - 输出开关字段齐全且命名统一
    - sim/real 评测模式能通过配置显式表达
  </acceptance_criteria>
  <done>后续实现可以围绕一套稳定配置展开，不再边写代码边发明字段。</done>
</task>

<task type="auto">
  <name>Task 2: 同步 recovery evaluation 配置校验</name>
  <files>src/contracts/validate_real_config.m, src/contracts/validate_sim_config.m</files>
  <read_first>config/default_real_config.m, config/default_sim_config.m</read_first>
  <action>把 recovery evaluation 的配置校验接入 real/sim validators，明确校验 `enable`、`evaluationMode`、case 列表、reference case 和 outputs 开关。非法值尽量 fail-fast，错误消息要能指出具体字段路径。</action>
  <verify>Get-Content src\contracts\validate_real_config.m, src\contracts\validate_sim_config.m | Select-String -Pattern "recoveryEvaluation","evaluationMode","savePanel","showPanel"</verify>
  <acceptance_criteria>
    - recovery evaluation 非法配置会在主流程前被拦截
    - `evaluationMode` 不合法时能直接报错
    - outputs 开关缺失或类型错误时能直接报错
  </acceptance_criteria>
  <done>recovery evaluation 契约在进入实现前就被 validators 稳定收口。</done>
</task>

<task type="auto">
  <name>Task 3: 同步配置与流程文档</name>
  <files>docs/config_contract.md, docs/pipeline_notes.md</files>
  <read_first>.planning/phases/06-analysis-and-hardening/06-RESEARCH.md</read_first>
  <action>更新 config 和 pipeline 文档，明确 recovery evaluation 是分析层独立模块，通过显式开关接入；写清楚固定案例、sim/real 共结构分口径、以及输出分类开关。</action>
  <verify>Get-Content docs\config_contract.md, docs\pipeline_notes.md | Select-String -Pattern "recoveryEvaluation","cases / comparisons / summary","savePanel","showPanel"</verify>
  <acceptance_criteria>
    - config 文档与默认配置一致
    - pipeline 文档明确 recovery evaluation 不是主链硬依赖
    - 输出开关和案例骨架在文档中可查
  </acceptance_criteria>
  <done>后续实现和测试都可以对齐同一份 recovery evaluation 契约说明。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `load_real_config()` 和 `load_sim_config()` 在新增 recoveryEvaluation 配置后可正常加载
- [ ] real/sim validators 能接受默认 recoveryEvaluation 配置
- [ ] 文档不再把 recovery evaluation 写成隐式或散落的分析逻辑
</verification>

<success_criteria>
- recovery evaluation 配置契约冻结完成
- validators 与文档同步完成
- 后续 Wave 2 可以直接围绕稳定字段实现主链
</success_criteria>

<output>
After completion, create `.planning/phases/06-analysis-and-hardening/06-01-SUMMARY.md`
</output>

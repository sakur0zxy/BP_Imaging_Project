# 05.1-03 Summary

## Outcome

- real/sim pipeline 已切到 `run_recovery`
- 新增 `tests/test_recovery_smoke.m`
- `tests/test_sim_pipeline.m` 已切到新 recovery 配置和新方法名
- 旧 `src/cs_recovery/` 与 `tests/test_cs_recovery_smoke.m` 已删除
- 正式代码和文档不再把 `run_cs_recovery` 作为官方接口

## Verification

- `matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests('tests'); disp(table(results)); assert(all([results.Passed]), 'Some tests failed.');"`

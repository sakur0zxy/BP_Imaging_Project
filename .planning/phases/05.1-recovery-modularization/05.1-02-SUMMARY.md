# 05.1-02 Summary

## Outcome

- 新建 `src/recovery/` 正式模块
- 官方入口切为 `run_recovery`
- 新增 `build_recovery_problem`、`prepare_recovery_config`、`get_recovery_registry`
- 新增 `shared/`、`result/`、`methods/cs/` 结构
- `cs_1d` / `cs_2d` 已迁移到新目录
- 公共 FISTA 求解器和 FFT 变换已下沉到 `methods/cs/`

## Verification

- `matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests({'tests/test_recovery_smoke.m'}); disp(table(results)); assert(all([results.Passed]), 'Some tests failed.');"`

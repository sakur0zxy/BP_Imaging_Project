---
phase: "05.1-recovery-modularization"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/default_real_config.m
  - config/default_sim_config.m
  - src/contracts/validate_real_config.m
  - src/contracts/validate_sim_config.m
  - docs/config_contract.md
  - docs/architecture_overview.md
autonomous: true
requirements:
  - ARCH-03
  - CS-01
  - CS-02
  - DOC-01
user_setup: []
---

<objective>
冻结新的 recovery 外部契约。  
Purpose: 先把新正式接口、配置结构和校验边界定死，避免后续模块迁移时继续围绕旧 `run_cs_recovery + 1d/2d + lambda1D/lambda2D` 打补丁。  
Output: 新 recovery 配置 schema、validators 和对应文档说明。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-recovery-modularization/05.1-CONTEXT.md
@.planning/phases/05.1-recovery-modularization/05.1-RESEARCH.md
@config/default_real_config.m
@config/default_sim_config.m
@src/contracts/validate_real_config.m
@src/contracts/validate_sim_config.m
@docs/config_contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将 recovery 默认配置切到长期结构</name>
  <files>config/default_real_config.m, config/default_sim_config.m</files>
  <read_first>.planning/phases/05.1-recovery-modularization/05.1-CONTEXT.md, docs/config_contract.md</read_first>
  <action>把 real/sim 默认配置中的 recovery 区块改成正式长期结构：`recovery.method = 'cs_1d'|'cs_2d'`，公共参数放到 `recovery.common.*`，方法专属参数放到 `recovery.methods.cs_1d.*` 与 `recovery.methods.cs_2d.*`。同时删掉旧的 `lambda1D`、`lambda2D`、旧方法名注释和旧字段说明，不保留 recovery 旧接口兼容。</action>
  <verify>Get-Content config\default_real_config.m, config\default_sim_config.m | Select-String -Pattern "cs_1d","recovery.common","recovery.methods.cs_1d","lambda1D","lambda2D"</verify>
  <acceptance_criteria>
    - 默认配置中 recovery 正式方法名为 `cs_1d`、`cs_2d`
    - 公共参数与方法参数分层清楚
    - recovery 旧扁平字段不再作为正式配置保留
  </acceptance_criteria>
  <done>后续代码和测试都有了唯一正式的 recovery 配置目标结构。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 recovery 配置校验契约</name>
  <files>src/contracts/validate_real_config.m, src/contracts/validate_sim_config.m</files>
  <read_first>config/default_real_config.m, config/default_sim_config.m</read_first>
  <action>把 real/sim 配置校验中的 recovery 逻辑同步切到新结构，明确校验 `method`、`common`、`methods.<method>` 三层，不再接受 `1d` / `2d` 和 `lambda1D` / `lambda2D`。报错信息保持简洁清楚，直接指出字段路径与错误原因。</action>
  <verify>Get-Content src\contracts\validate_real_config.m, src\contracts\validate_sim_config.m | Select-String -Pattern "cs_1d","cs_2d","recovery.common","lambda1D","lambda2D"</verify>
  <acceptance_criteria>
    - recovery 校验只接受新方法名和新配置结构
    - 非法方法名与缺失参数能 fail-fast
    - 错误消息中包含清楚字段路径
  </acceptance_criteria>
  <done>recovery 新契约在主流程执行前就能被稳定拦截和校验。</done>
</task>

<task type="auto">
  <name>Task 3: 同步 recovery 契约文档</name>
  <files>docs/config_contract.md, docs/architecture_overview.md</files>
  <read_first>.planning/phases/05.1-recovery-modularization/05.1-RESEARCH.md</read_first>
  <action>把 `docs/config_contract.md` 和 `docs/architecture_overview.md` 中的 recovery 结构说明切到新正式命名，明确 `src/recovery/` 是长期模块、`run_recovery` 是唯一入口，并说明 `methods/cs/` 是当前唯一已实现的算法族。</action>
  <verify>Get-Content docs\config_contract.md, docs\architecture_overview.md | Select-String -Pattern "src/recovery","run_recovery","cs_1d","cs_2d"</verify>
  <acceptance_criteria>
    - 文档中的 recovery 契约与默认配置一致
    - 架构文档不再把 `src/cs_recovery/` 写成正式层
    - 新方法命名和目录职责说明完整
  </acceptance_criteria>
  <done>后续实现与测试都能对齐同一份恢复层正式契约。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `load_real_config()` 与 `load_sim_config()` 在新 recovery 结构下可成功加载
- [ ] `validate_real_config()` 与 `validate_sim_config()` 能接受 `cs_1d/cs_2d`，拒绝旧 `1d/2d`
- [ ] 文档中不再把 `lambda1D/lambda2D` 作为正式 recovery 字段
</verification>

<success_criteria>
- real / sim recovery 配置统一切到新正式结构
- recovery validators 与文档同步完成
- 后续模块迁移不再受旧 recovery 命名牵制
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-recovery-modularization/05.1-01-SUMMARY.md`
</output>

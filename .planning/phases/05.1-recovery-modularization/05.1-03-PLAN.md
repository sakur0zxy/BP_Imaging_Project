---
phase: "05.1-recovery-modularization"
plan: "03"
type: execute
wave: 3
depends_on:
  - "05.1-02"
files_modified:
  - src/pipelines/run_real_data_pipeline.m
  - src/pipelines/run_sim_data_pipeline.m
  - tests/test_recovery_smoke.m
  - tests/test_real_pipeline.m
  - tests/test_sim_pipeline.m
  - docs/pipeline_notes.md
  - docs/architecture_overview.md
  - src/cs_recovery/build_measurement_model.m
  - src/cs_recovery/recover_1d.m
  - src/cs_recovery/recover_2d.m
  - src/cs_recovery/run_cs_recovery.m
  - tests/test_cs_recovery_smoke.m
autonomous: true
requirements:
  - CS-01
  - CS-02
  - TEST-01
  - DOC-01
user_setup: []
---

<objective>
把主流程、测试和文档切到新的 recovery 模块，并清理旧 `cs_recovery` 入口。  
Purpose: 结束恢复层的“双轨接口”状态，让仓库只剩下一套正式 recovery 结构。  
Output: real/sim pipeline 全部切到 `run_recovery`，测试与文档同步，旧 `src/cs_recovery/` 被移除。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-recovery-modularization/05.1-CONTEXT.md
@src/pipelines/run_real_data_pipeline.m
@src/pipelines/run_sim_data_pipeline.m
@tests/test_cs_recovery_smoke.m
@docs/pipeline_notes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 切换 real/sim pipeline 到新 recovery 入口</name>
  <files>src/pipelines/run_real_data_pipeline.m, src/pipelines/run_sim_data_pipeline.m</files>
  <read_first>src/recovery/run_recovery.m, .planning/phases/05.1-recovery-modularization/05.1-02-PLAN.md</read_first>
  <action>把 real/sim pipeline 中的恢复接入点从 `run_cs_recovery(...)` 切到 `run_recovery(...)`，同步调整日志、summary、method 名称与 recovery 状态字段，确保主流程不再引用旧 `1d/2d` 和旧 `cs_recovery` 路径。</action>
  <verify>Get-Content src\pipelines\run_real_data_pipeline.m, src\pipelines\run_sim_data_pipeline.m | Select-String -Pattern "run_recovery","run_cs_recovery","cs_1d","cs_2d"</verify>
  <acceptance_criteria>
    - real/sim pipeline 都只调用 `run_recovery`
    - 日志与 summary 不再写旧 `1d/2d` 正式方法名
    - pipeline 行为保持 `degradation -> recovery -> bp_imaging` 不变
  </acceptance_criteria>
  <done>主流程与恢复层的耦合点切换到新正式入口。</done>
</task>

<task type="auto">
  <name>Task 2: 重写 recovery 测试并覆盖主流程集成</name>
  <files>tests/test_recovery_smoke.m, tests/test_real_pipeline.m, tests/test_sim_pipeline.m</files>
  <read_first>tests/test_cs_recovery_smoke.m, src/recovery/run_recovery.m</read_first>
  <action>把旧 `test_cs_recovery_smoke.m` 重写为新的 `test_recovery_smoke.m`，并同步更新 real/sim pipeline 测试，使它们使用新 recovery 配置结构和 `cs_1d/cs_2d` 方法名。测试要覆盖入口调度、1D/2D 恢复冒烟，以及 pipeline 集成不回退到旧接口。</action>
  <verify>Get-Content tests\test_recovery_smoke.m, tests\test_real_pipeline.m, tests\test_sim_pipeline.m | Select-String -Pattern "run_recovery","cs_1d","cs_2d","run_cs_recovery"</verify>
  <acceptance_criteria>
    - recovery 冒烟测试只基于新入口和新方法名
    - real/sim pipeline 测试覆盖新 recovery 集成路径
    - 测试代码中不再依赖旧扁平 recovery 配置
  </acceptance_criteria>
  <done>测试层与主流程一起切换到同一套恢复契约。</done>
</task>

<task type="auto">
  <name>Task 3: 清理旧 recovery 目录并收口文档</name>
  <files>docs/pipeline_notes.md, docs/architecture_overview.md, src/cs_recovery/build_measurement_model.m, src/cs_recovery/recover_1d.m, src/cs_recovery/recover_2d.m, src/cs_recovery/run_cs_recovery.m, tests/test_cs_recovery_smoke.m</files>
  <read_first>docs/config_contract.md, docs/pipeline_notes.md, docs/architecture_overview.md</read_first>
  <action>删除旧 `src/cs_recovery/` 下的正式入口与方法文件，清理旧测试文件，并把 pipeline / architecture 文档收口到 `src/recovery/` 新结构。执行一次全文搜索，确认仓库中不再有旧 `run_cs_recovery`、旧 `recovery.method = '1d'|'2d'` 作为正式接口残留。</action>
  <verify>Get-ChildItem -Recurse -File | Select-String -Pattern "run_cs_recovery|recovery.method = '1d'|recovery.method = '2d'|lambda1D|lambda2D" | ForEach-Object { '{0}:{1}:{2}' -f $_.Path, $_.LineNumber, $_.Line.Trim() }</verify>
  <acceptance_criteria>
    - 仓库中不再保留旧 `src/cs_recovery/` 正式入口
    - pipeline / architecture 文档全部切到 `src/recovery/`
    - 全文搜索确认旧 recovery 正式接口已清理干净
  </acceptance_criteria>
  <done>仓库只剩下一套正式 recovery 架构，不再存在旧新并存状态。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 目标搜索中不再出现旧 `run_cs_recovery` 正式引用
- [ ] MATLAB 测试至少覆盖 `test_recovery_smoke.m`、`test_real_pipeline.m`、`test_sim_pipeline.m`
- [ ] real/sim 默认配置都能以新 recovery 配置结构正常加载并跑到主流程
</verification>

<success_criteria>
- real/sim pipeline 与 tests 全部切到 `run_recovery`
- 旧 `src/cs_recovery/` 正式接口被清理
- recovery 文档、架构说明和测试一起收口到新结构
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-recovery-modularization/05.1-03-SUMMARY.md`
</output>

# 05.1-01 Summary

## Outcome

- 默认配置已切到长期 recovery 结构：`recovery.common.* + recovery.methods.*`
- real/sim 配置校验已切到 `prepare_recovery_config`
- `docs/config_contract.md`、`docs/architecture_overview.md`、`docs/pipeline_notes.md` 已同步到新 recovery 契约
- `.planning/REQUIREMENTS.md` 中的正式分层已从 `cs_recovery` 改为 `recovery`

## Verification

- `matlab -batch "cd('E:/博士文件/工作整理/2026/BP_Imaging_Project'); startup; results = runtests('tests'); disp(table(results)); assert(all([results.Passed]), 'Some tests failed.');"`

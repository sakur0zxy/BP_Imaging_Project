---
phase: "05.1-recovery-modularization"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05.1-01"
files_modified:
  - src/recovery/run_recovery.m
  - src/recovery/build_recovery_problem.m
  - src/recovery/prepare_recovery_config.m
  - src/recovery/get_recovery_registry.m
  - src/recovery/shared/resolve_observation_mask.m
  - src/recovery/shared/get_method_config.m
  - src/recovery/result/compute_recovery_metrics.m
  - src/recovery/result/pack_recovery_result.m
  - src/recovery/methods/cs/recover_cs_1d.m
  - src/recovery/methods/cs/recover_cs_2d.m
  - src/recovery/methods/cs/solvers/run_fista_sparse_recovery.m
  - src/recovery/methods/cs/transforms/transform_azimuth_fft.m
  - src/recovery/methods/cs/transforms/inverse_transform_azimuth_fft.m
  - src/recovery/methods/cs/transforms/transform_fft2d.m
  - src/recovery/methods/cs/transforms/inverse_transform_fft2d.m
autonomous: true
requirements:
  - ARCH-03
  - CS-01
  - CS-02
user_setup: []
---

<objective>
建立新的 `src/recovery/` 模块骨架，并把现有 CS 方法迁进统一架构。  
Purpose: 让恢复层真正从“CS 专用脚本集合”变成“通用恢复模块 + 当前已实现的 CS family”。  
Output: `run_recovery` 统一入口、统一 `recoveryResult`、新的 `methods/cs/` 目录和共享 solver / transforms。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-recovery-modularization/05.1-CONTEXT.md
@.planning/phases/05.1-recovery-modularization/05.1-RESEARCH.md
@src/cs_recovery/run_cs_recovery.m
@src/cs_recovery/build_measurement_model.m
@src/cs_recovery/recover_1d.m
@src/cs_recovery/recover_2d.m
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 recovery 顶层入口与统一问题构造</name>
  <files>src/recovery/run_recovery.m, src/recovery/build_recovery_problem.m, src/recovery/prepare_recovery_config.m, src/recovery/get_recovery_registry.m, src/recovery/shared/resolve_observation_mask.m, src/recovery/shared/get_method_config.m</files>
  <read_first>.planning/phases/05.1-recovery-modularization/05.1-CONTEXT.md, src/cs_recovery/run_cs_recovery.m, src/cs_recovery/build_measurement_model.m</read_first>
  <action>实现新的 recovery 顶层入口和基础骨架：`run_recovery` 负责配置准备、问题构造、方法分发和跳过逻辑；`build_recovery_problem` 负责把 `sourceData/referenceSourceData` 整理成统一恢复问题；`get_recovery_registry` 提供当前支持的方法注册信息。所有外部分支判断都收口到这一层。</action>
  <verify>Get-Content src\recovery\run_recovery.m, src\recovery\build_recovery_problem.m, src\recovery\get_recovery_registry.m | Select-String -Pattern "cs_1d","cs_2d","recoveryResult","build_recovery_problem"</verify>
  <acceptance_criteria>
    - `run_recovery(...)` 成为唯一正式恢复入口
    - 恢复问题构造与方法分发职责分开
    - 跳过逻辑与方法注册逻辑收口到 recovery 顶层
  </acceptance_criteria>
  <done>主流程以后只需要依赖一个稳定 recovery 入口。</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 CS family 并抽公共求解器</name>
  <files>src/recovery/methods/cs/recover_cs_1d.m, src/recovery/methods/cs/recover_cs_2d.m, src/recovery/methods/cs/solvers/run_fista_sparse_recovery.m, src/recovery/methods/cs/transforms/transform_azimuth_fft.m, src/recovery/methods/cs/transforms/inverse_transform_azimuth_fft.m, src/recovery/methods/cs/transforms/transform_fft2d.m, src/recovery/methods/cs/transforms/inverse_transform_fft2d.m</files>
  <read_first>src/cs_recovery/recover_1d.m, src/cs_recovery/recover_2d.m</read_first>
  <action>把旧 1D/2D 实现迁成 `recover_cs_1d.m` 和 `recover_cs_2d.m`，将重复的 FISTA 主循环抽到 `methods/cs/solvers/run_fista_sparse_recovery.m`，把 1D/2D 稀疏变换下沉到 `methods/cs/transforms/`。方法文件只保留“本方法的差异”，不再复制整套 solver。</action>
  <verify>Get-Content src\recovery\methods\cs\recover_cs_1d.m, src\recovery\methods\cs\recover_cs_2d.m, src\recovery\methods\cs\solvers\run_fista_sparse_recovery.m | Select-String -Pattern "run_fista_sparse_recovery","transform_azimuth_fft","transform_fft2d"</verify>
  <acceptance_criteria>
    - `recover_cs_1d` 与 `recover_cs_2d` 不再各自内嵌完整 FISTA 主循环
    - solver 与 transform 职责拆开
    - 新方法命名与 registry 保持一致
  </acceptance_criteria>
  <done>CS 家族被整理成一个可继续扩展的算法子树，而不是平铺脚本。</done>
</task>

<task type="auto">
  <name>Task 3: 统一 recovery 结果封装与指标输出</name>
  <files>src/recovery/result/compute_recovery_metrics.m, src/recovery/result/pack_recovery_result.m</files>
  <read_first>src/cs_recovery/run_cs_recovery.m, src/cs_recovery/recover_1d.m, src/cs_recovery/recover_2d.m</read_first>
  <action>把 observed consistency、missing / whole relative error 等结果整理成统一 `result/` 层，由 recovery 顶层统一封装 `recoveryResult`。不同方法不再各自决定最终返回结构，确保 pipeline 永远拿到同样的字段。</action>
  <verify>Get-Content src\recovery\result\compute_recovery_metrics.m, src\recovery\result\pack_recovery_result.m | Select-String -Pattern "observedConsistencyErr","missingRelErr","wholeRelErr","sourceData"</verify>
  <acceptance_criteria>
    - 所有方法输出都统一封装成同一结构
    - metrics 的字段口径统一
    - `sourceData.echo` 与 `sourceData.meta.recovery` 的更新位置收口到统一结果封装层
  </acceptance_criteria>
  <done>后续 pipeline、tests 和 analysis 可以依赖稳定的 recoveryResult 契约。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 可以通过 `run_recovery(..., method='cs_1d', ...)` 完成一次恢复
- [ ] 可以通过 `run_recovery(..., method='cs_2d', ...)` 完成一次恢复
- [ ] 观测样本保持不变，统一 metrics 字段存在且数值合理
</verification>

<success_criteria>
- `src/recovery/` 成为新的正式恢复模块
- `cs_1d` / `cs_2d` 在新结构下跑通
- 公共 solver / transform / result 层落地
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-recovery-modularization/05.1-02-SUMMARY.md`
</output>
